import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    maxWidth: 800,
    width: '100%',
    minHeight: '100vh',
  },
}));

export default useStyles;
